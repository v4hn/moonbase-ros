# they add hg commit sums to the folder name - we don't want that here
mk_source_dir &&
cd $SOURCE_DIRECTORY &&
tar -x --strip-components=1 -f $SOURCE_CACHE/$SOURCE &&

sedit 's/XML_NO_ERROR/XML_SUCCESS/' gazebo/util/LogPlay.cc
