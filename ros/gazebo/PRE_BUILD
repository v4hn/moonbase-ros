# they add hg commit sums to the folder name - we don't want that here
mk_source_dir &&
cd $SOURCE_DIRECTORY &&
tar -x --strip-components=1 -f $SOURCE_CACHE/$SOURCE &&

sedit 's@gdal/@@' gazebo/common/{Dem.cc,DemPrivate.hh,Dem.hh} gazebo/physics/HeightmapShape.cc &&

# we're not ubuntu...
sedit 's@/share/OGRE/cmake/modules@/lib/OGRE/cmake@' cmake/gazebo-config.cmake.in
